#pragma once 

namespace Compile_Time {

	template<class subtype = nullptr_t>
	struct u8;

	template<class subtype = nullptr_t>
	struct u16;

	template<class subtype = nullptr_t>
	struct u32;

	template<class subtype = nullptr_t>
	struct u64;

	template<class subtype = nullptr_t>
	struct s8;

	template<class subtype = nullptr_t>
	struct s16;

	template<class subtype = nullptr_t>
	struct s32;

	template<class subtype = nullptr_t>
	struct s64;

	template<class subtype = nullptr_t>
	struct Bit;
}